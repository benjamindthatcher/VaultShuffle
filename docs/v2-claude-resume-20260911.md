# Claude handoff — 11 September 2026 London

**Superseded at01:10UTC:** Claude exhausted, all88native tasks finished,
auto-continueoff, no-overlap handoff message43 verified. Existing max Luna
workers again own all three batches below. Do not resume Claude work until
root reconciles and explicitly transfers ownership.

User says Claude is back. All three Codex max Luna workers are now confirmed
errored at their account limit, so no overlapping implementation is running.
Claude parent remains Opus 5 / Ultracode; native agents Opus 5 / max. Resume
from saved files, not the old unfinished requests. Work in large autonomous
batches and bank checkpoints before limits. Root retains milestone signoff and
remote actions. Overall M3 is not complete; production is unchanged.

## What completed since your last session

- M3 physical final4 passed fresh PG17 replay, all new SQL fixtures, tenant
  security/deletion/retention checks, and root's separate local SQL rerun.
- Native column-specific SET NULL preserves required tenant IDs on collection/
  draw deletion. UI-history retention is latest100/90days; NOT MATERIALIZED
  ranked CTE permits account predicate pushdown, redundant indexes removed.
  Actual multi-account EXPLAIN proves only the selected account is ranked.
- Identity/scalars and sessions/capabilities passed root's **40 combined tests**
  after review. Preserve manual login sessions; only security intents expire at
  freeze. Proven deleted tombstones carry explicit proof, emit no invented
  capability tuple, and cannot own a valid session. Null maps return stable
  errors. Leave these completed modules stable.
- Manifest full gate **119 Python tests**, coverage44relations/486columns.
  Final-load intentionally blocks1hourscolumn/5relationdecisions pending source
  snapshot/cutover evidence. No real export or source snapshot accepted.

## Critical applied migration state

The user explicitly approved the reviewed M3 rehearsal migration, and root
successfully applied it on VaultShuffle2 `vbjtbwelnhbbdfrqczyf` at
10 September23:26:54UTC. Management assigned version `20260910232654`.
Local SQL was RENAMED, not edited, to:

`database/v2/supabase/migrations/20260910232654_m3_preservation_schema.sql`

SHA256 `605b72a3d9df1328ec27033c3420c1813a238f6e15bd8fc28f971cddaf30a24a`.
Contract before path/applied-state doc refresh:
`46e9b982395cb2800da1247d22afc43de0e1cc3e99df1fc632a7b032142a3577`.
**All M1/M2/M3 migrations are immutable. No edit/reapply.** New physical gaps
require a new separately reviewed migration; report concrete evidence to root.
Marker deliberately remainsm1, targetaccounts/games0. Root still needs remote
schema/security/rollback checks. The first remote table tally omitted support;
all six private schemas app/catalog/reco/ops/migration/support should be92/991.
Supabase connector vanished again after apply; do not assume callable access.

## Three disjoint native batches

1. **Catalogue/game completion review and mapping metadata refresh.** Own
   `games*`, `catalogue*`, their tests/contracts/checkpoint and
   `database/v2/migration/**` only. Existing
   `docs/v2-m3-catalogue-checkpoint.md` says bounded implementation complete:
   66catalogue tests,26value tests, strictTS/lint, actual disposablePG constraint
   inserts. Game-map suite and completed API claims need one finished review,
   not repeated implementation. Review preserved every catalog_games column,
   explicit destination gaps, exact PGarray/JSON numbers, deterministic map
   and USprice facts. Fix real blockers, validate and return. Update migration
   index builder/tests from oldM3 filename/proposed to new applied filename;
   regenerate sidecar/disposition manifest at exact immutable hash and rerun
   coverage/119testbaseline. May update M3-contract/checkpoint references and
   add applied-state note, but **never edit SQL**. Those doc/index edits belong
   only to this worker for this batch; historical handoffs may remain historical.
2. **Library/family/history completion.** Own existing `library*`, `family*`,
   `history*`, tests and docs/v2-m3-library-{contract,checkpoint}.md. Continue
   M3-F from exact current files and common GameMap API. The checkpoint was
   missing at the last allowance stop, so bank current state first. Session/
   capability fixes are complete and outside this scope. Finish typed outputs,
   exact measurements/authored-state distinctions, unknowncompletionhistory,
   stale rawaudit versus sparse evidence, tests and actual local constraints.
3. **Collections/commitments/draw-history implementation.** Own only new
   `collections*`, `commitments*`, `draws*`, tests and commitmentcontract/
   checkpoint. Complete docs/v2-m3-g-commitment-transform-batch.md from current
   files (may be early/no implementation). Bank accurate checkpoint first.
   Coordinate GameMap, library-row-map and catalogue exactvalue APIs directly
   with their owners once. No sibling/shared edits without agreement.

Read M3-F/M3-G full assignments and shared
`docs/v2-claude-domain-transforms.md`. Earlier Luna ownership banners are now
superseded by this handoff. Root owns the plan and `v2-execution-status.md`.
Parent owns a new `docs/v2-claude-20260911-checkpoint.md` for coordination and
one completed integration review after batch returns. No routine supervision.

## Boundaries and validation

Current branch `codex/v2-architecture`, HEAD
`79010ab31e751141057f8f255af3d6affdd35da7`. User switched to main and made a footer
commit; root fast-forwarded v2 to preserve it and switched back at identical
tree. No v2commit/push. Preserve all dirty/user work. No remote/source queries,
export, credentials/auth mutation, target load/apply, provider activation,
application/deployment changes or production write. Local synthetic testing
authorized; own isolated fixture, never disturb sibling/M1/M2 evidence.
Shared PG17 prefix `node_modules/.cache/vaultshuffle-pg17-20260910`, read-only.
Source audit counts are not snapshot parity or immutable preconditions.

Return exact completed tests/files/physical blockers and remaining domains;
do not call M3 complete until consistent realexport, all transforms, peraccount
parity/reconciliation/checksums and measuredstorage pass. Remaining later work
includes duration/provider evidence, config/recommendation/support/ops
transforms, staged loader and parity harness. Avoid inventing missing source
facts merely to satisfy immutable destination bounds. When allowance ends,
bank actual work and stop; root will transfer ownership explicitly.
