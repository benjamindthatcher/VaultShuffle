# Historical Claude coordination checkpoints — 11 September 2026

Superseded by the Codex dispatch in the current execution ledger. These are
chronological records, not current ownership or instructions.

## Current checkpoint — 11 September 2026

**Dispatch verified, 11 September about16:00UTC:** both next batches are
assigned. First thread: Sonnet5/Medium, M3-H provider/catalogue plus cache fix,
message53. Second thread: preservation final corrections, **Opus5/High**
visually verified in Claude after compaction saved442.5K context tokens
(460.8K ->20.5K). The correction prompt is visibly sent and the task is
running. Its prompt initially named SonnetHigh while the model menu was
unresponsive; the final actual selector is Opus5/High, as intended for these
data-integrity defects. No further steering is needed. Wait for completed
returns, per the user's explicit instruction; do not poll intermediate work.

CUA recovery note: AX snapshots/menu indices became stale and reported a
model menu while the real UI had moved on. Resetting the CUA binding and
using its supported screenshot revealed the actual sent/running state and
Opus5/High selection. Prefer a screenshot promptly if AX actions appear not
to take effect, rather than repeating clicks. Both conversations and all
code remained intact; no duplicate follow-up was sent.

**Latest completed review and next batches:** catalogue correction returned
142 tests and clean TS/lint, but root found the frozen-map eligibility scan
still runs before each cache hit, making repeated lookup linear. First Claude
thread is now running message53 at Sonnet5/Medium under
`docs/v2-m3-h-provider-transform-batch.md`: fix that bounded regression, then
implement the nine remaining catalogue/provider evidence relations.

Preservation follow-up returned **125 unit / 26 actual PG tests**, strict
TS/lint, and an **unapplied** proposal at
`database/v2/proposals/m3_legacy_preservation_followup.sql`; detailed return is
`docs/v2-m3-preservation-followup-checkpoint.md`. Root has not accepted it:
the NULL-loss exception admits legacy Owned as well as Wishlist, divergent
recency facts still live only in expiring staging, and baseline suppression
claims an active-library destination even for rows that do not enter it.
The retired-family access fix and segregated completion exception direction
remain useful. Exact next correction is
`docs/v2-m3-preservation-final-review.md`, same preservation file ownership;
use Opus5/High for these data-integrity defects, no native agents. The second
thread's old 460K context is being compacted (message5) before dispatch;
verify the model switch and next prompt if this continuation is interrupted.
No SQL promotion/apply, generated-index change or remote action occurred.

**LATEST handoff and cost policy:** the user switched root from Astra Ultra to
**Astra High** and explicitly requested no continuous supervision. Leave
delegated batches to finish, then review completed results; wait without
routine UI/status polling. All older fixed max/Ultracode defaults remain
superseded by dynamic, lowest-sufficient model/effort selection.

The completed implementation return reports **418 combined transform tests**,
**25 library/history PG tests**, **129 M3-G unit tests and 12 M3-G PG tests**,
repository typecheck and v2 lint passing. Catalogue's three root correction
findings were not included in that return. Root compacted the oversized first
Claude conversation and queued message50 to execute only that correction at
Sonnet5/Medium; the task is running.

Root reviewed the second thread's finished preservation report and found
unsupported source assumptions: actual personal-import and family-removal
writers use Wishlist for removed-access tombstones, so it does not prove a
game was never owned; recency timestamp relationships are not schema
guarantees. Binding corrections and the next coherent batch are in
`docs/v2-m3-preservation-followup-batch.md`. Message3 is verified sent/running
in the second Sonnet5/High thread, which now owns library/family/history,
paired tests/docs and a new local SQL proposal/test under
`database/v2/proposals/`. It must preserve normal runtime constraints, prevent
retired family access from becoming active, separate recency receipt/play
facts, and quarantine inverted completion clocks with a final-load blocker.
No applied SQL/index edits, remote apply or production changes are authorized
in that batch. These two owners do not overlap. Review after their completed
returns; do not resume old Codex workers or native Claude agents.

**Active Claude threads — 11 September, about 10:44 UTC:** implementation
remains “M3 migration readiness audit”, Sonnet5/Medium, verified. Library batch
returned 121 unit tests and **25 actual PG17 constraint tests**, strict TS/lint;
it has advanced to commitments/draws, then catalogue corrections. A second
fresh thread, **“VaultShuffle v2-m3 legacy gap review”**, Sonnet5/High,
`local_9219c373-fc88-401a-9568-9294c8e44389`, is running a bounded read-only
design review of the three physical preservation cases. It owns only
`docs/v2-m3-legacy-gap-review.md`, under
`docs/v2-m3-legacy-gap-review-batch.md`; no code, tests or SQL overlap. This
second perspective is for concrete data-integrity decisions, not repeated
implementation review. Maximum two Claude threads, no native agents. Its first
prompt is verified; a duplicate unsent draft was cleared. Prior Codex workers
remain stopped. Preserve these owners when resuming.

**LATEST policy and ownership — 11 September 10:31 UTC:** user replaces the
fixed max-agent strategy with dynamic model/effort selection. Prioritize Claude
while allowance is available; one Claude thread by default, two only for
distinct useful purposes, no native subagent fan-out. Simple work uses cheaper
low/medium effort; normal implementation uses capable medium; escalate only
for concrete complexity/risk. The old three max Luna / Opus Ultracode defaults
are superseded. Codex fallback workers follow the same cost-conscious policy.

Catalogue Luna returned **128 tests** (34 games / 68 catalogue / 26 values),
strict TS/lint, and applied-index finalization. Root completed-review acceptance
is still pending. The library and commitment Lunas subsequently hit allowance
limits; all Codex workers are stopped. Claude is available again, and the
existing task is set to **Sonnet 5 / Medium** for sequential completion of the
unfinished library/family/history and collections/commitments/draws batches.
No implementation overlaps. Exact current handoff and boundaries:
[efficient Claude continuation](v2-claude-efficient-resume-20260911.md).
Library checkpoint has 121 tests and a new local constraint harness on disk;
M3-G has implementations and some tests, but its checkpoint is stale and final
validation remains unfinished. Root retains catalogue integration review,
source prerequisites, ledger and milestone signoff. Never infer completion
from the historical ownership entries below.

Claude message45 is verified sent and running at Sonnet5/Medium. Root's
completed catalogue boundary review reproduced three local defects: mismatched
map/source provenance accepted, malformed external stub title accepted, and
stale cached IDs after a mutable map changes. Exact bounded follow-up is in
[catalogue review](v2-m3-catalogue-review.md). Root has stopped editing that
domain; the same Claude thread can take these corrections after the current
two batches. Catalogue acceptance remains pending those fixes, with no second
reviewer or model escalation needed. No new remote action occurred.

